Release 1.0.6

This repository has been tagged with version 1.0.6.

How to install:

- Via Composer (when published on Packagist):

```bash
composer require oidc-client/sdk:^1.0
```

Local usage instructions:
- Run `composer install` in the project root to install dependencies.
- See `docs/integration/` for framework-specific instructions.

Tagging and publishing notes:
- A Git tag `v1.0.0` has been created in this repository.
- To publish to Packagist, submit the repository and release accordingly.
